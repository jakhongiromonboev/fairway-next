import { NextPage } from 'next';
import withLayoutBasic from '../../libs/components/layout/LayoutBasic';

const ProductsPage: NextPage = () => {
	return <div>HELLO FROM PRDUCTS</div>;
};

export default withLayoutBasic(ProductsPage);
